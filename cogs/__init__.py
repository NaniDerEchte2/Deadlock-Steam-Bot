"""Deadlock bot cogs package (Steam subset).

This file ensures that the `cogs` directory is treated as a regular Python
package so module paths like `cogs.steam` resolve when used from the
Hauptbot."""
